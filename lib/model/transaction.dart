class TransactionExp {
  String id;
  String title;
  double amount;
  DateTime timestamp;
  String emoji;
  String store;
  TransactionExp(
      {this.id,
      this.title,
      this.amount,
      this.timestamp,
      this.emoji,
      this.store});
}
